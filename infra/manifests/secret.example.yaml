# Reference only. The Helm chart (infra/helm) renders this Secret from the
# `hf_token` value, so you normally do not apply this file by hand.
# HUGGING_FACE_HUB_TOKEN is optional — the default models are ungated; set it only
# for a gated model. To use it manually: copy to secret.yaml, fill in, apply.
# Do not commit secret.yaml.

apiVersion: v1
kind: Secret
metadata:
  name: vllm-secrets
  namespace: workshop
type: Opaque
stringData:
  HUGGING_FACE_HUB_TOKEN: ""
